<script setup lang="ts">
import { PropType } from "vue";
import { MessageTypes, MsgType } from "../utils/types";

defineProps({
  text: { type: String, required: true },
  type: { type: Object as PropType<MsgType>, default: MessageTypes.default },
});
</script>

<template>
  <transition
    enter-active-class="transition duration-100 ease-out"
    enter-from-class="transform scale-95 opacity-0"
    enter-to-class="transform scale-100 opacity-100"
    leave-active-class="transition duration-300 ease-out"
    leave-from-class="transform scale-100 opacity-100"
    leave-to-class="transform scale-95 opacity-0"
  >
    <div class="text-white flex p-4 gap-5 items-center" :class="type.color">
      <component :is="type.icon" class="w-8 h-8"></component>
      <p class="text-xl">
        {{ text }}
      </p>
    </div>
  </transition>
</template>
